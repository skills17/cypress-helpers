# yaml-language-server: $schema=https://schema.skills17.ch/task-config/v3/config.schema.json
id: config-display-name
serve:
  enabled: true
groups:
- match: A.+
- match: B.+
  displayName: BGroup::foo
- match: C.+
  displayName: Last group
